import React from 'react'
import { Modal } from 'semantic-ui-react'

function ModelEdit({ isOpen }) {
    return (
        <Modal open={isOpen}>
        </Modal>
    )
}

export default ModelEdit
