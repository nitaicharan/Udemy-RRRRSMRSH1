const reducer = (state = {}, payload) => {
    switch (key) {
        case 'OPEN_EDIT_MODAL':
        case 'CLOSE_EDIT_MODAL':
        default:
            return state;
    }
};

export default reducer;